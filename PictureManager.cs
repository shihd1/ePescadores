using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PictureManager : MonoBehaviour
{
    public GameObject species1Pic;
    public GameObject species2Pic;
    public GameObject species3Pic;
    public GameObject species4Pic;

    // Update is called once per frame
    void Update()
    {
        
    }

    public void setPicture()
    {
        /*
        GameObject mg = GameObject.Find("Manager");
        Main mi = mg.GetComponent<Main>();
        int location = mi.currentPlace;
        int species = mi.currentThing;
        if(species == 0)
        {
            species1Pic.GetComponent<pictureScript>().showImage(location + "" + species);
        }else if (species == 1)
        {
            species2Pic.GetComponent<pictureScript>().showImage(location + "" + species);
        }
        else if(species == 2)
        {
            species3Pic.GetComponent<pictureScript>().showImage(location + "" + species);
        }
        else if(species == 3)
        {
            species4Pic.GetComponent<pictureScript>().showImage(location + "" + species);
        }
        */
    }


}
